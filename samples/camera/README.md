# ESP32 Camera Samples

这里放的是之前 ESP32 设备流程真实拍到的 JPEG 图片，用作 `/camera/upload` 的本地开发测试夹具。

| 文件 | 当前人工测试标签 |
| --- | --- |
| `shuyao_chuilin_sheng_ding_esp32.jpg` | `shuyao_chuilin_sheng_ding` |
| `yingguo_jade_eagle_esp32.jpg` | `yingguo_jade_eagle` |

当前阶段标签仍然是人工指定的。目标是先验证后端上传、设备上下文和聊天追问链路，后续再把人工标签替换成真实视觉模型输出。
